{
  "name": "root",
  "private": true,
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "29.2.2",
    "ts-jest": "29.0.3",
    "ts-node": "10.9.1",
    "typescript": "4.8.4"
  },
  "scripts": {
    "sdk:dev": "yarn workspace @beanstalk/sdk dev",
    "sdk:build": "yarn workspace @beanstalk/sdk build",
    "sdk:test": "jest --selectProjects sdk --",
    "generate": "yarn workspace @beanstalk/sdk generate",
    "test": "jest"
  }
}
